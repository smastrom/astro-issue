---
import Layout from '../layouts/Layout.astro'

// await new Promise((r) => setTimeout(r, 1500))

const data = await fetch('https://jsonplaceholder.typicode.com/todos').then((res) => res.json())
---

<Layout>
   <div>
      <h1>About</h1>
      {data.map((t) => <p>{t.title}</p>)}
   </div>
</Layout>
